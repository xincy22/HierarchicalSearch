# 层级检索（Hierarchical Retrieval）方案

> 目标：在**无多轮对话、PDF 标题编号风格不统一**的前提下，实现一个**单链、稳定、可解释、可实践**的层级检索模块。
> 核心思路：**先定位文档 doc_id → 再定位章节 section_id（锚点优先，向量兜底）→ 拉取该节正文内容**。

------

## 1. 背景与问题复盘

### 1.1 当前模式的根因问题

原先做法是：把 `filename + file_topic + l1 + l2` 拼起来做 embedding 存向量库，再用向量库“检索层级信息”。

这在“标题指向型”问题上还能工作，但在“明确锚点型”问题上天然不稳，例如：

- “XXX论文 2.1 讲了什么”
  向量库能锁定 XXX 文档没问题，但**2.1 这种结构化锚点**很可能不在 topK 里。
  原因很简单：**embedding 对数字、标号、括号编号等 token 的表达并不可靠**，而且拼接字段会让“文档信息”压过“章节差异”。

### 1.2 设计目标（必须满足的工程约束）

- **无多轮对话**：不能依赖当前章节状态（“这一节/上一节”不在边界内）
- **PDF 标题前缀风格多样**：1 / 一 / (1) / （一） / 2.1 / 2-1 / 第2章 ……
- **系统偏好单链**：对外只暴露一次调用链，内部可做优先级逻辑，但不能演化成复杂多分支系统
- **必须闭环**：检索输出能直接拿到正文（不能只返回标题字符串）
- **必须可诊断**：出错能定位到 doc 定位错 / section 定位错 / 入库解析错

------

## 2. 边界定义（层级检索能处理什么、不能处理什么）

### 2.1 处理范围（本方案覆盖）

仅覆盖两类问题：

1. **明确锚点型**
   用户给出明确定位信号：`2.1`、`3.2.1`、`第2章`、`（一）`、`(1)` 等
   目标：定位到某个 `section_id` 并返回该节内容。
2. **标题指向型**（fallback）
   用户没有给锚点，但提到章节名称或关键词：
   “实验设置那一节”“相关工作部分”“结论怎么说”
   目标：通过章节标题向量检索定位 `section_id`。

### 2.2 不处理范围（明确排除）

- “这一节/上一节/刚才那部分”：无多轮状态，无法可靠定位
- 跨文档事实问答：属于 general retrieval，不由层级检索主导
- 用户无任何线索且无关键词：无法保证唯一定位（只能返回低置信或走通用检索）

------

## 3. 总体架构：两级索引 + 一张章节表

本方案把检索拆成两层索引：

- **文档级索引（doc_vectors）**：解决 doc_id（书名简称/缩写等问题）
- **章节级索引（section_vectors）**：解决 section_id（标题指向型 fallback）
- **MySQL sections 表**：存储 `doc_id + section_id` 对应的正文内容（最终落点）

对外呈现为**单链调用**：

1. 得到 `doc_id`
2. 尝试通过 LLM 得到 `section_id`（锚点主路径）
3. 校验存在性，不行则向量检索 fallback
4. 取 MySQL 正文

------

## 4. 数据模型与存储设计

### 4.1 MySQL：documents（文档元信息表）

用途：提供稳定 `doc_id`，与向量库、章节表做关联。

**字段（最小集）：**

- `doc_id`（PK，自增或 UUID）
- `filename`（原文件名）
- `file_topic`（LLM 抽取的主题）
- `doc_title`（可选：LLM抽取的书名/文章标题）
- `created_at`

> 注意：MySQL 不负责“用户输入的书名匹配”，它只提供主键与元数据。匹配交给 `doc_vectors`。

------

### 4.2 MySQL：sections（章节内容表）

用途：以 `(doc_id, section_id)` 为联合主键，直接返回正文。

**字段：**

- `doc_id`（PK part）
- `section_id`（PK part）例如：`2` / `2.1` / `2.1.3`
- `level`（建议存：1/2/3；不存也能从点号推断）
- `title_text`（去除前缀后的纯标题）
- `body_text`（该 section 正文）

- `heading_raw`（原始标题行，保留前缀，如“（一） 实验设置”）
- `heading_prefix_raw`（提取的前缀：`（一）`、`2.1`等）
- `start_pos` / `end_pos`（可选：在 markdown 中的行号或 offset，方便排查切分边界问题）

**索引：**

- `PRIMARY KEY (doc_id, section_id)`
- 可选：`INDEX(doc_id, level)`（便于按层级取目录）

------

### 4.3 Milvus：doc_vectors（文档级向量集合，新增）

用途：解决 doc_id 获取，覆盖简称、缩写、别名、英文名等。

**字段：**

- `doc_id`
- `vector`
- （可选）`variant`：`title/topic/alias`（用于调试）

**向量文本：**

- `filename + "\n" + file_topic`
- 可选：为同一 doc 生成多条 alias 向量（仍然存同 doc_id，不需要新表）

------

### 4.4 Milvus：section_vectors（章节级向量集合）

用途：标题指向型 fallback，范围内（doc 内）找 section。

**字段：**

- `doc_id`
- `section_id`
- `l1_title`
- `l2_title`
- `l3_title`
- `vector`

**向量文本内容：**

- 不要放 `filename/file_topic`（doc 已经锁定，放进去只会干扰区分）
- 放“层级拼接标题”：
  - L1：`l1_title`
  - L2：`l1_title + "\n" + l2_title`
  - L3：`l1_title + "\n" + l2_title + "\n" + l3_title`

------

## 5. section_id 设计与生成规则

### 5.1 编号规则

只使用数字路径：`^\d+(\.\d+)*$`，例：`1`, `2`, `2.1`, `2.1.3`

### 5.2 Chapter 0

让“摘要/前言/引言”等前置模块可被稳定检索。

将第一个一级标题之前的内容识别为以下的几种类别：

- Abstract / 摘要
- Preface / 前言 / 序 / 序言
- Introduction / 引言 / 导言

### 5.3 检索阶段的 section_id 使用规则

检索阶段的目标：**尽量利用用户提供的结构信息（章/节编号）来直接定位 `section_id`，但绝不在信息不完整或歧义时“硬猜”。**

因此，在线部分的 `section_id` 生成规则要非常保守：

> 只要觉得“有可能指向多个章节”，就一律认为信息不足（INSUFFICIENT），交给标题向量 fallback。

以下几类输入，信息足够明确，可以生成 `section_id`：

**显式数字路径类**

- 示例：
  - “2.1 讲了什么”
  - “3.2.1 的内容”
  - “第 2.1 节说了什么”
- 处理：
  - 识别出 `2.1` / `3.2.1`
  - 做简单归一化（去掉空格、全角点转半角点、`2-1` → `2.1`）
  - 直接作为 section_id 候选值

**明确的“第 X 章 / 第 X 章第 Y 节”链路**

- 示例：
  - “第 2 章 讲了什么” → `2`
  - “第二章 第一节 内容” → `2.1`
  - “第 3 章 第 2 节 第 1 小节 讲了什么” → `3.2.1`
- 处理：
  - 识别出“第 X 章、第 Y 节、第 Z 小节”等结构
  - 将中文数字转成阿拉伯数字（“第二章”→2，“第三节”→3）
  - 拼成数字路径 `X` / `X.Y` / `X.Y.Z` 作为 `section_id`

**Chapter 0 的固定映射**

- 示例：
  - “摘要讲了什么”
  - “前言主要说什么”
  - “引言部分的内容”
- 前提：入库阶段已经固化：
  - 摘要 → `0.1`
  - 前言 → `0.2`
  - 引言 → `0.3`
- 处理：
  - 按约定映射到对应 `0.x` 作为 `section_id`

以下情况，一律认为**无法安全生成 section_id**，直接输出 `INSUFFICIENT`，进入向量 fallback 流程：

1. **只出现关键词，没有编号**
   - 例：
     - “实验设置这一节讲了什么”
     - “相关工作那部分”
     - “方法这一章”
   - 问题：可能有多个“实验设置”“相关工作”“方法”，无法唯一对应一个 `section_id`。
2. **只出现“节/章”而无序号**
   - 例：
     - “第一节讲了什么”（没有“第几章”的上下文，且无多轮对话）
   - 问题：可能在多处都有“第一节”。
3. **只有不可靠前缀**
   - 例：
     - “（一）实验设置讲了什么”
     - “(1) 部分内容”
   - 问题：原 PDF 的“（一）”可能出现在多个层级和位置 → 无法反推唯一 `section_id`。

------

## 6. 入库流程（离线）— 从 PDF 到 MySQL + Milvus

你现有逻辑基本可用，本方案只把 doc_id 与 section_id 固化补齐。

### 6.1 文档入库

1. PDF → markdown
2. 抽取 `file_topic`（LLM）
3. 写 `documents`，得到 `doc_id`
4. 写 `doc_vectors`
   - embedding 文本：`filename + "\n" + file_topic (+ doc_title)`

**可选强化：**

- LLM 生成 5～10 个别名/简称（如“计算机组成”“计组”“组成原理”等）
- 每个别名写一条 `doc_vectors`（同一个 `doc_id`，多条向量）

这会显著提高 doc_id 的召回稳定性。

------

### 6.2 章节入库

1. 抽候选标题行
2. LLM 判定 L1 标题集合并编号（+ Chapter 0）
3. LLM 在每个 L1 区间判定 L2 标题集合并编号
4. 为每个章节节点生成并固化 `section_id`
5. 按章节边界切分正文 → 得到 `body_text`
6. 写 MySQL `sections(doc_id, section_id, title_text, body_text, ...)`
7. 写 Milvus `section_vectors(doc_id, section_id, l1_text, l2_text, l3_text, vector)`
   - embedding 文本按第 4.4 节建议构造

------

## 7. 在线检索流程（单链实现）

### 7.1 总流程

1. **Doc 定位**：从 query 得到 doc_id
2. **锚点定位（LLM）**：让 LLM 输出 section_id 或 INSUFFICIENT
3. **存在性校验**：MySQL 验证 `(doc_id, section_id)` 是否存在
4. **Fallback（向量）**：如不足或不存在，则在 doc 内向量检索 section
5. **取正文**：MySQL 拉取 body_text

------

### 7.2 Step 1：doc_id 获取（向量检索或混合检索）

**检索：** Milvus `doc_vectors` topK（建议 20）  
**聚合：** 以 doc_id 聚合（同 doc 多条 alias 向量时必需   
**重排：**请求 LLM 进行相关性排序  
**输出：** doc_id（top1），可保留 top2 做兜底  

------

### 7.3 Step 2：LLM 生成 section_id

LLM 只做一件事：输出 `section_id` 或 `INSUFFICIENT`。

**强约束：**

- section_id 必须匹配正则：`^\d+(\.\d+)*$`
- 否则当作 `INSUFFICIENT`

**输出格式：**

```json
{"section_id":"2.1"}
```

或

```json
{"section_id":"INSUFFICIENT"}
```

------

### 7.4 Step 3：存在性校验

```sql
SELECT 1
FROM sections
WHERE doc_id = ? AND section_id = ?
LIMIT 1;
```

- 存在：直接进入 Step 5 取正文
- 不存在：进入 Step 4 fallback

------

### 7.5 Step 4：fallback（标题指向型向量检索）

**检索：**在 `section_vectors` 中检索，但必须 `doc_id` 过滤：

- filter：`doc_id = <selected_doc_id>`
- topK：20～50

**重排：**将 query 和 entities 请求 LLM 进行相关性排序

**输出：**取 Top 1

------

### 7.6 Step 5：取正文

```sql
SELECT title_text, body_text
FROM sections
WHERE doc_id = ? AND section_id = ?
LIMIT 1;
```

------

## 8. 最终交付物清单

### 数据库与索引

-  MySQL 建表：documents、sections
-  sections 联合主键：`(doc_id, section_id)`

### 向量库集合

-  Milvus：doc_vectors
-  Milvus：section_vectors

### 入库流水线改造

-  生成 doc_id（写 documents）
-  写 doc_vectors（可选 alias 多向量）
-  生成 section_id（入库固化、冲突处理）
-  写 sections
-  写 section_vectors

### 在线检索实现

-  doc_vectors 检索 → doc_id
-  LLM 输出 section_id / INSUFFICIENT（严格 JSON + 正则校验）
-  MySQL 校验存在性
-  不存在则 section_vectors fallback
-  MySQL 拉正文